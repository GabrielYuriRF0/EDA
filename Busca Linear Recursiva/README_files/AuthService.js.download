app.service("AuthService", ["$http", "SessionService", function($http, SessionService) {

	var self = this;

	self.login = function () {
        // Session.create(res.data.id, res.data.user.id,
        //                res.data.user.role);
        // return res.data.user;
    };

}]);